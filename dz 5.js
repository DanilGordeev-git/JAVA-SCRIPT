// Входные данные
let n = 12;
let x = 2;
let y = 6;

// Проверка деления нацело
let isDivisible = n % x === 0 && n % y === 0;

// Вывод результата
console.log(`n = ${n}, x = ${x}, y = ${y} => ${isDivisible}`);
